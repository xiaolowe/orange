<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="site.model">	<!-- <sql id="t_site_model_column">		 t.id, t.link, t.name, t.code, t.img, t.email, t.qq, t.wx, t.qqId, t.wxId, t.qqNick, t.wxNick, t.avator,		 t.status, t.expertIds, t.shoperIds, t.createTime, t.updateTime, t.lat, t.lng, province, t.city, t.district, t.addr,		 t.roleCode, t.domains, t.elink, t.slink, t.type, t.profile, t.gender	</sql>-->	<select id="site.model.selectPageList" parameterType="com.orange.site.entity.ModelEntity"		resultType="hashMap">		select t.*		from t_site_model t where 1=1		<if test="name!=null and name!=''">			and name like CONCAT('%','${name}','%' )		</if>		<if test="status!=null and status!=''">			and status=#{status}		</if>		order by orders		limit #{offset},#{pageSize}	</select>	<select id="site.model.selectPageCount" parameterType="com.orange.site.entity.ModelEntity"		resultType="java.lang.Integer">		select count(*) from t_site_model where 1=1		<if test="name!=null and name!=''">			and name like CONCAT('%','${name}','%' )		</if>		<if test="status!=null and status!=''">			and status=#{status}		</if>	</select>	<insert id="site.model.insert" parameterType="com.orange.site.entity.ModelEntity"		useGeneratedKeys="true" keyProperty="id">		insert into t_site_model		(		id		<if test="name!=null and name!=''">			,name		</if>		<if test="code!=null and code!=''">			,code		</if>		<if test="status!=null and status!=''">			,status		</if>		<if test="orders!=null and orders!=''">			,orders		</if>		) values(		id		<if test="name!=null and name!=''">			,#{name}		</if>		<if test="code!=null and code!=''">			,#{code}		</if>		<if test="status!=null and status!=''">			,#{status}		</if>		<if test="orders!=null and orders!=''">			,#{orders}		</if>		)	</insert>	<update id="site.model.update" parameterType="com.orange.site.entity.ModelEntity">		update t_site_model		set id=#{id}		<if test="name!=null and name!=''">			,name=#{name}		</if>		<if test="code!=null and code!=''">			,code=#{code}		</if>		<if test="status!=null and status!=''">			,status=#{status}		</if>		<if test="orders!=null and orders!=''">			,orders=#{orders}		</if>		where 1=1 and id=#{id}	</update>	<select id="site.model.selectList" parameterType="com.orange.site.entity.ModelEntity"		resultType="com.orange.site.entity.ModelEntity">		select t.*		from t_site_model t where 1=1		<if test="name!=null and name!=''">			and name like CONCAT('%','${name}','%' )		</if>		<if test="status!=null and status!=''">			and status=#{status}		</if>		order by orders	</select>	<select id="site.model.selectOne" parameterType="com.orange.site.entity.ModelEntity"		resultType="com.orange.site.entity.ModelEntity">		select t.*		from t_site_model t where 1=1		<if test="id!=null and id!=''">			and id=#{id}		</if>	</select>	<select id="site.model.selectById" parameterType="string"		resultType="com.orange.site.entity.ModelEntity">		select t.*		from t_site_model t where 1=1 and id=#{id}	</select>	<delete id="site.model.deleteById" parameterType="com.orange.site.entity.ModelEntity">		delete from		t_site_model where 1=1 and id=#{id}	</delete></mapper>
package com.orange.cas.action;import com.orange.cas.entity.LogEntity;import com.orange.cas.service.LogService;import com.orange.core.base.dao.page.PagerModel;import com.orange.web.action.base.BaseAction;import com.orange.web.action.common.statics.Commons;import com.orange.web.action.holder.RequestHolder;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Controller;import org.springframework.web.bind.annotation.RequestMapping;import org.springframework.web.bind.annotation.RequestParam;/** * 系统日志管理 */@Controller@RequestMapping("/cas/log/")public class LogAction extends BaseAction<LogEntity> {		    @Autowired	private LogService casLogService;    private static final String page_toList = Commons.SCOPE_MANAGE + "/cas/log/logList";    private static final String page_toAdd = Commons.SCOPE_MANAGE + "/cas/log/logEdit";    private static final String page_listAndDetail = Commons.SCOPE_MANAGE + "/cas/log/logListAndDetail";    public LogAction() {        super.page_toList = page_toList;        super.page_toEdit = page_toAdd;        super.page_toAdd = page_toAdd;    }    @Override	public LogService getService() {		return casLogService;	}    @Override	protected void selectListAfter(PagerModel pager) {		pager.setPagerUrl("selectList");	}    @Override	public void insertAfter(LogEntity e) {		e.clear();	}    	@RequestMapping("logListAndDetail")	public String logListAndDetail(@RequestParam(required = false)Integer type, LogEntity e) throws Exception{//		if(type == null){//			e.setType(Integer.valueOf(type));//		}		selectList(RequestHolder.getRequest(), e);		return page_listAndDetail;	}}